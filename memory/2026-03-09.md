# 2026-03-09

- Worked extensively on `~/Desktop/franchise-simulator` (frontend + backend), not `~/.openclaw/workspace/bazi-frontend` for the user’s “franchise simulator” requests.
- Identified recurring Run Simulation failure root cause: header/menu button called submit handler without guaranteed event object. Stable fix is event-safe submit handling and/or direct run function.
- User requested rollback due to breakage; production `main` was force-reset to older commit and later patched. Important pattern: keep `main` stable, use `dev` for preview testing.
- Created/pushed `dev` branch and set upstream for safer Vercel preview workflow (`dev` for testing, `main` for production).
- Implemented major UI features in Franchise Simulator:
  - Recalculation behavior and loading modal patterns
  - History card compare selection (green primary, yellow secondary)
  - Dedicated Compare Scenarios page (left: 2-column history cards; right: graph dashboard + narrative text report)
  - Filters/sort/pagination on comparison page history list
  - Typography/UI polish (Arial body, Poppins headings, clearer section borders)
- Repeatedly deployed via Vercel from `frontend/` and confirmed alias to `https://frontend-lime-three-81.vercel.app`.
- Added backend platform scaffolding for public SaaS model:
  - Auth endpoints (register/login/me) with JWT
  - Basic user plan model (free/pro)
  - Stripe checkout + webhook scaffolding
  - Plan-gated report behavior (free gets shallower AI output)
  - Simulation storage endpoints with per-plan limits
- Implemented 6-part “AI Investment Analysis Dashboard Structure” in Financial Actions tab:
  1) Investment Overview
  2) Financial Health
  3) Top Risk Drivers
  4) Market Snapshot
  5) Sensitivity Analysis
  6) AI Investment Verdict
  while keeping financial calculations deterministic and AI as interpretation/recommendation only.
